<thlogic>
    <attr sel="#mainHeader"
          th:text="#{page.admin.main.header}"/>

    <attr sel="#adminAbilities"
          th:text="#{page.admin.abilities}"/>

    <attr sel="#deleteHistoryCommands"
          th:text="#{page.admin.history.configs}"
            th:href="@{__${T(cz.muni.ics.web.utils.ViewLinks).ADMIN_HISTORY}__}"/>

    <attr sel="#importBtn"
          th:text="#{page.import}"
          th:href="@{__${T(cz.muni.ics.web.utils.ViewLinks).IMPORT_START}__}"/>
</thlogic>