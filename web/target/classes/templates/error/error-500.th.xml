<thlogic>
    <!--Variables-->
    <attr sel="#reasonHeading"
          th:text="#{page.error.server}"/>

    <attr sel="#reasonException"
          th:text="${request.getAttribute('javax.servlet.error.exception')}"/>

    <attr sel="#homeBtn"
          th:href="@{__${T(cz.muni.ics.web.utils.ViewLinks).HOME}__}"/>

    <attr sel="#linkText"
          th:text="#{page.home}"/>

</thlogic>